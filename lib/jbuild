(jbuild_version 1)

(library
  ((name bitarray)
   (c_names (bit_array bitarray_stubs))
   (libraries (ctypes bitarray_bindings))))

(rule
  ((targets (bitarray_stubs.c bitarray_generated.ml))
   (deps (../lib_gen/bitarray_bindgen.exe))
   (action (run ${<}))))
